import React from 'react'

const CardCollection = () => {
    return(
        <div>
            will map over a user's collected cards and display them using card components here.
        </div>
    )
}

export default CardCollection